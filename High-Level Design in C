#include <stdio.h>

// Function to multiply two integers
int multiply(int a, int b) {
    return a * b;
}

int main() {
    int a = 5, b = 6;
    int product = multiply(a, b);
    printf("Product: %d\n", product);
    return 0;
}
